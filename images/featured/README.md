Masthead logos for the "FEATURED IN" band on the home page.

Drop logo files here (transparent-background PNG or SVG), then add one `<img>` line
inside `<div class="featured-logos">` in `/index.html`. The band is hidden until the
`hidden` attribute is removed from `<section class="featured-in">` in that same file.
